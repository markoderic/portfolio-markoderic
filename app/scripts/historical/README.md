Historical scenarios only. Milestone 1 and scene-refinement checks predate brief 11 and contain obsolete expectations (single window, old toolbar, reference phone). They are retained for provenance, not as the current specification. Do not execute them under the existing browser URL-policy restriction.

Current non-browser suite: `node --test app/scripts/verify-workspace.mjs`.
Current browser acceptance remains the manual sequence in `docs/redesign/12-immersive-workspace-progress.md`; none of those steps is counted as passed until observed.
