import fs from 'node:fs';import {deskPose} from '../src/prototype/deskCamera.js';
const dir=new URL('../../docs/redesign/session-56-shadow-continuity/',import.meta.url),old=new URL('../session-55-prop-placement/',dir),path=new URL('current-scene.json',dir),scene=JSON.parse(fs.readFileSync(path));
for(const m of scene.meshes){if(m.root==='desk'&&Math.max(...m.positions.map(p=>p[0]))>5.39&&Math.min(...m.positions.map(p=>p[0]))< -5.39){m.wood=true;m.color=[1,1,1];m.uv=m.positions.map(p=>[p[0]/10.8+.5,p[2]/6+.5]);}for(const key of ['positions','normals','colors','uv'])if(m[key])m[key]=m[key].map(a=>a.map(n=>+n.toFixed(6)));}fs.writeFileSync(path,JSON.stringify(scene));
const desk=deskPose({width:1200,height:800}),frames=[];for(const stage of ['before','after'])for(const night of [false,true])frames.push({name:stage+'-'+(night?'night':'day')+'-desk',stage,night,fraction:0,pose:'pose-000',eye:desk.position.toArray(),look:desk.look.toArray(),width:1200,height:800,theme:'light',texture:'screen.png'});fs.writeFileSync(new URL('frames.json',dir),JSON.stringify(frames,null,2));for(const file of ['screen.png','top.png'])fs.copyFileSync(new URL(file,old),new URL(file,dir));
let s=fs.readFileSync(new URL('render-study.swift',old),'utf8').replace('let ground=base.appendingPathComponent("../session-39-depth-shadows/after-maps")','let ground=base.appendingPathComponent(frame["pose"] as! String)').replace('base.appendingPathComponent(stage+"-scene.json")','base.appendingPathComponent("current-scene.json")').replace('base.appendingPathComponent(stage+"-measurements.json")','base.appendingPathComponent("current-measurements.json")').replace('let vertices=original','let vertices=(m["drawer"] as? Int == 2) ? original.map{SCNVector3($0.x,$0.y,$0.z+CGFloat(frame["fraction"] as! Double)*2.35)} : original').replace('for m in maps where m["name"] as! String != "table-contact" {','for m in maps where m["name"] as! String != "table-contact" && (!before || m["name"] as! String != "directional") {').replace('(m["name"] as! String)=="contact" ? 2 : 1','(m["name"] as! String)=="directional" ? 0 : ((m["name"] as! String)=="contact" ? 2 : 1)').replace('only camera prop and supported microphone change','only the computed directional ground shadow layer changes').replace('SceneKit + actual top map + static screen','SceneKit + CPU ground maps + static screen').replace('Not browser output. Actual generated top map; edge blend/roughness, lighting and shadows approximated. Same viewing camera/materials; only the computed directional ground shadow layer changes. No native acceptance.','Not browser output. Same geometry/camera/lighting. CPU ground contacts + unfiltered directional silhouette; native PCF, GPU motion and appearance unverified.');
fs.writeFileSync(new URL('render-study.swift',dir),s);
