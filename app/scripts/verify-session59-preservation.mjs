import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {fileURLToPath} from 'node:url';
const root=fileURLToPath(new URL('../../',import.meta.url)),dir=path.join(root,'docs/redesign/session-59-fan-entry-cable'),baseline=JSON.parse(fs.readFileSync(path.join(root,'docs/redesign/morning-baseline-2026-09-21/before-hashes.json'))),hash=p=>createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const changes=[];let production=0,sourcePackage=0;for(const [p,h]of Object.entries(baseline)){const actual=hash(path.join(root,p));if(actual!==h)changes.push(p);if(p.startsWith('app/dist/')){assert.equal(actual,h,p);production++;}if(p.startsWith('app/prototype-vendor/')){assert.equal(actual,h,p);sourcePackage++;}}
assert.deepEqual(changes.sort(),['app/src/prototype/Scene.jsx','app/src/prototype/productionPropsGeometry.js','app/src/prototype/runtimeRevision.js']);
const build=path.join(root,'app/.vite/production-check-session59');let canonical=0;for(const rel of fs.readdirSync(build,{recursive:true})){const p=path.join(build,rel);if(!fs.statSync(p).isFile())continue;assert.equal(hash(p),hash(path.join(root,'app/dist',rel)),rel);canonical++;}
const result={changedBaselineFiles:changes,existingProductionOutputsUnchanged:production,freshCanonicalProductionOutputsMatch:canonical,unchangedVendorFiles:sourcePackage,sourceArchiveSync:'deferred to final cross-session checkpoint; archive is unchanged Session58 source, not current repaired source',listener:'Parent process-only check found previous PID91415 gone and no5175listener; not restarted in59',nativeAcceptance:'not performed'};fs.writeFileSync(path.join(dir,'preservation.json'),JSON.stringify(result,null,2));console.log(result);
